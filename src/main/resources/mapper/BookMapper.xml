<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.BookDao">

    <select id="getBook" parameterType="String" resultType="com.example.demo.entity.Book">
        select * from t_book where book = #{bookName}
    </select>


    <insert id="insertBook" parameterType="com.example.demo.entity.Book" >
        insert into t_book(book,price) values(#{book},#{price})
    </insert>
    <insert id="baseInsert" parameterType="com.example.demo.entity.Book" >
        insert into t_book(book,price) values(#{book},#{price})
    </insert>
    <!--    <insert id="insert" parameterType="cn.gausscode.calo.user.entity.SysUserEntity">-->
    <!--        insert into sys_user (name,login_name,email,tel,password,pic_url,site_id,create_date,create_by,update_date,update_by,del_flag,remarks)-->
    <!--                values (#{name},#{loginName},#{email},#{tel},#{password},#{picUrl},#{siteId},#{createDate},#{createBy},#{updateDate},#{updateId},#{delFlag},#{remarks})-->
    <!--    </insert>-->

    <!--    <update id="delete" parameterType="int">-->
    <!--        update sys_user set del_flag = 0 where id = #{id}-->
    <!--    </update>-->

    <!--    <update id="update" parameterType="cn.gausscode.calo.user.entity.SysUserEntity">-->
    <!--        update sys_user set-->
    <!--            name = #{name},-->
    <!--            login_name = #{loginName},-->
    <!--            email = #{email},-->
    <!--            tel = #{tel},-->
    <!--            password = #{password},-->
    <!--            pic_url = #{picUrl},-->
    <!--            site_id = #{siteId},-->
    <!--            create_date = #{createDate},-->
    <!--            create_by = #{createBy},-->
    <!--            update_date = #{updateDate},-->
    <!--            update_by = #{updateId},-->
    <!--            del_flag = #{delFlag},-->
    <!--            remarks = #{remarks}-->
    <!--        where id = #{id}-->
    <!--    </update>-->
</mapper>
